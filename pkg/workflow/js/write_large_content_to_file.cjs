const fs=require("fs"),path=require("path"),crypto=require("crypto"),{generateCompactSchema}=require("./generate_compact_schema.cjs");function writeLargeContentToFile(content){const logsDir="/tmp/gh-aw/safeoutputs";fs.existsSync(logsDir)||fs.mkdirSync(logsDir,{recursive:!0});const filename=`${crypto.createHash("sha256").update(content).digest("hex")}.json`,filepath=path.join(logsDir,filename);return fs.writeFileSync(filepath,content,"utf8"),{filename,description:generateCompactSchema(content)}}module.exports={writeLargeContentToFile};