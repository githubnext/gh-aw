const fs=require("fs");function loadConfig(configPath){if(!fs.existsSync(configPath))throw new Error(`Configuration file not found: ${configPath}`);const configContent=fs.readFileSync(configPath,"utf-8"),config=JSON.parse(configContent);if(!config.tools||!Array.isArray(config.tools))throw new Error("Configuration must contain a 'tools' array");return config}module.exports={loadConfig};