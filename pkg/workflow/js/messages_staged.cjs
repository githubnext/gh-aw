const{getMessages,renderTemplate,toSnakeCase}=require("./messages_core.cjs");function getStagedTitle(ctx){const messages=getMessages(),templateContext=toSnakeCase(ctx);return renderTemplate(messages?.stagedTitle?messages.stagedTitle:"## 🏴‍☠️ Ahoy Matey! Staged Waters: {operation} Preview",templateContext)}function getStagedDescription(ctx){const messages=getMessages(),templateContext=toSnakeCase(ctx);return renderTemplate(messages?.stagedDescription?messages.stagedDescription:"🗺️ Shiver me timbers! The following booty would be plundered if we set sail (staged mode disabled):",templateContext)}module.exports={getStagedTitle,getStagedDescription};