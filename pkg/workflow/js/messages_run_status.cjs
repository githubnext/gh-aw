const{getMessages,renderTemplate,toSnakeCase}=require("./messages_core.cjs");function getRunStartedMessage(ctx){const messages=getMessages(),templateContext=toSnakeCase(ctx);return renderTemplate(messages?.runStarted?messages.runStarted:"⚓ Avast! [{workflow_name}]({run_url}) be settin' sail on this {event_type}! 🏴‍☠️",templateContext)}function getRunSuccessMessage(ctx){const messages=getMessages(),templateContext=toSnakeCase(ctx);return renderTemplate(messages?.runSuccess?messages.runSuccess:"🎉 Yo ho ho! [{workflow_name}]({run_url}) found the treasure and completed successfully! ⚓💰",templateContext)}function getRunFailureMessage(ctx){const messages=getMessages(),templateContext=toSnakeCase(ctx);return renderTemplate(messages?.runFailure?messages.runFailure:"💀 Blimey! [{workflow_name}]({run_url}) {status} and walked the plank! No treasure today, matey! ☠️",templateContext)}function getDetectionFailureMessage(ctx){const messages=getMessages(),templateContext=toSnakeCase(ctx);return renderTemplate(messages?.detectionFailure?messages.detectionFailure:"⚠️ Security scanning failed for [{workflow_name}]({run_url}). Review the logs for details.",templateContext)}module.exports={getRunStartedMessage,getRunSuccessMessage,getRunFailureMessage,getDetectionFailureMessage};