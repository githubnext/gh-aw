--- pkg/workflow/create_discussion.go
+++ pkg/workflow/create_discussion.go
@@ -90,6 +90,7 @@ func (c *Compiler) parseDiscussionsConfig(outputMap map[string]any) *CreateDisc
 
 // Parse expires field (days until discussion should be closed)
 if expires, exists := configMap["expires"]; exists {
+discussionLog.Printf("DEBUG: Found expires in configMap, value=%v, type=%T", expires, expires)
 switch v := expires.(type) {
 case int:
 discussionsConfig.Expires = v
