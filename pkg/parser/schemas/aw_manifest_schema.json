{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["name"],
  "properties": {
    "manifest-version": {
      "type": "string",
      "enum": ["1"]
    },
    "min-version": {
      "type": "string",
      "description": "Minimum supported gh-aw CLI version. Requires the exact vMAJOR.minor.patch form.",
      "pattern": "^v[0-9]+\\.[0-9]+\\.[0-9]+$"
    },
    "max-version": {
      "type": "string",
      "description": "Maximum supported gh-aw CLI version. Requires the exact vMAJOR.minor.patch form.",
      "pattern": "^v[0-9]+\\.[0-9]+\\.[0-9]+$"
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "emoji": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "license": {
      "type": "string",
      "description": "SPDX license identifier for the package.",
      "minLength": 1,
      "maxLength": 64,
      "pattern": "^[A-Za-z0-9.+-]+$"
    },
    "tags": {
      "type": "array",
      "description": "Up to 10 short package tags, each at most 32 characters.",
      "maxItems": 10,
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 32
      }
    },
    "categories": {
      "type": "array",
      "description": "Package category classification.",
      "items": {
        "type": "string",
        "enum": [
          "automation",
          "ci-cd",
          "code-review",
          "security",
          "documentation",
          "release",
          "triage",
          "testing"
        ]
      }
    },
    "includes": {
      "type": "array",
      "description": "Installable package entries. Use folder naming conventions to infer type: workflows under workflows/, agentic-workflows/, or .github/workflows/; skill directories under skills/ or .github/skills/; agent files under agents/ or .github/agents/.",
      "items": {
        "type": "string"
      }
    },
    "files": {
      "type": "array",
      "description": "Deprecated. Use 'includes' instead.",
      "items": {
        "type": "string"
      }
    },
    "skills": {
      "type": "array",
      "description": "Skill directory paths relative to the package root. Each entry is a directory that must contain a SKILL.md file; all direct files in the directory are installed to the agentic engine skill folder.",
      "items": {
        "type": "string"
      }
    },
    "agents": {
      "type": "array",
      "description": "Agent markdown file paths relative to the package root. Each entry must be a .md file under an agents/ directory; the file is installed to the agentic engine agents folder.",
      "items": {
        "type": "string"
      }
    }
  }
}
