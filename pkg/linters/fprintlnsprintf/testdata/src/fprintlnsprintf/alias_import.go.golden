package fprintlnsprintf

import (
	f "fmt"
	"os"
)

// aliased import: f.Fprintln(w, f.Sprintf(...)) should still be flagged.
func flaggedAlias(name string) {
	f.Fprintf(os.Stderr, "hello %s\n", name) // want "use fmt.Fprintf"
}
