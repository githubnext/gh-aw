{
  "name": "gh-aw-actions",
  "version": "1.0.0",
  "description": "Custom GitHub Actions for gh-aw safe outputs",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:release": "RELEASE_MODE=true node build.js"
  },
  "devDependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^2.0.0",
    "@actions/github": "^6.0.1",
    "esbuild": "^0.19.0"
  }
}
