<playwright-output>
<path>/tmp/gh-aw/mcp-logs/playwright/</path>
<description>When using Playwright tools to take screenshots or generate files, all output files are automatically saved to this directory. This is the Playwright --output-dir and you can find any screenshots, traces, or other files generated by Playwright in this directory.</description>
</playwright-output>

<playwright-networking>
<host>host.docker.internal</host>
<description>The Playwright MCP server runs inside a Docker container. When using Playwright to navigate to local services (for example, a development server running on the GitHub Actions runner), use `host.docker.internal` as the hostname. For example, use `http://host.docker.internal:8080`. The MCP is mounted with `--host` and is accessible through `host.docker.internal`, not `localhost`.</description>
</playwright-networking>
