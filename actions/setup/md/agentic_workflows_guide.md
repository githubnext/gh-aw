<agentic-workflows-guide>
Use MCP tools (NOT shell commands): use `status` to list workflows, use `logs` to find runs, use `audit` for a deep dive into a specific run, use `compile` to recompile workflows.
</agentic-workflows-guide>
