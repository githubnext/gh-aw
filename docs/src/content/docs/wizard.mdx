---
title: AW wizard
description: Client-side wizard shell preview for building a gh-aw prompt from the shared JSON data model.
sidebar:
  label: AW wizard
  order: 22
---

import Wizard from '../../components/Wizard.astro';

## AW wizard preview

This page hosts the client-side shell for the docs wizard. It loads its options from the shared JSON data model and demonstrates the step container, progress UI, navigation, and in-memory selection flow that later issues will extend with richer step content.

<Wizard />
