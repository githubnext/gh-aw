{
  "version": 4,
  "note": "SUPERSEDED by state.json (source of truth). Kept as a tiny pointer only.",
  "scope": "eslint-factory (repo root); targets actions/setup/js/**/*.cjs",
  "approach": "Each run: read state.json+history.jsonl -> re-read eslint-factory/src to catch newly-shipped rules -> pick newest/least-reviewed rule -> ground findings via grep on live .cjs corpus (npm/lint blocked) -> dedupe via gh api issues endpoint -> file <=3 issues -> update memory -> push.",
  "see": "state.json for rules_inventory, open issues, ops notes, nextPriorities."
}
