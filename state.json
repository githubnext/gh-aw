{
  "lastRun": "2026-07-05",
  "ruleCount": 9,
  "rules": ["no-core-setoutput-non-string", "no-unsafe-catch-error-property", "no-unsafe-promise-catch-error-property", "prefer-get-error-message", "prefer-number-isnan", "require-async-entrypoint-catch", "require-await-core-summary-write", "require-json-parse-try-catch", "require-parseInt-radix"],
  "newRuleThisPeriod": "prefer-number-isnan added since 2026-07-04 run (was 8, now 9). Not in prior state.json rules list; never reviewed before. type=suggestion, warn.",
  "shipped_since_last_run": "Commit bd0c249 (#43404) fixed the await-summary alias FN #1 I filed 2026-07-04 (local `summary = core.summary` aliases now detected). Issue #43326 is my await-summary FN3 (non-bare-call statement positions) - still OPEN.",
  "focusThisRun": "First review of NEW 9th rule prefer-number-isnan. Filed 2 grounded refinement issues (kept to 2, not 3, to avoid dilution).",
  "issuesFiledThisRun": [
    "prefer-number-isnan HIGH (grounded 51 sites): misleading 'wrap with Number(...)' caveat + suggestion-only. 0/51 live isNaN sites hit the coercion footgun - EVERY arg is provably numeric (parseInt/parseFloat/Number/.getTime result). For those the transform is a pure equivalence but rule offers no autofix and attaches a wrong caveat. Ask: detect provably-numeric arg -> safe `fix` + caveat-free message; keep suggest-only+caveat for unknown args; add idiom tests.",
    "prefer-number-isnan MED (ungrounded/latent): hasLocalBinding excludes ImportBinding defs (d.type !== 'ImportBinding') -> import-bound isNaN (lodash) / window / global get flagged as the real global. No ESM consumers in corpus (CJS only), no test, undocumented rationale. Ask: document+test OR drop the filter so imports count as shadows."
  ],
  "preferNumberIsNanFindings": {
    "corpus": "51 global isNaN() call sites in actions/setup/js; 12 Number.isNaN sites; 0 isFinite; 0 aliased/destructured isNaN bindings; 0 raw-string isNaN args (all args are parseInt/parseFloat/Number/.getTime results).",
    "ruleCatchesWell": "bare isNaN(x), globalThis/window/global .isNaN and [\"isNaN\"] computed forms, scope-aware shadow exclusion (const isNaN=Number.isNaN skipped). Suggestion (not fix) correctly gated behind semantic-flip caveat.",
    "dominantIdiom": "const n = parseInt(String(v),10); if (isNaN(n)) ... -> isNaN===Number.isNaN here (arg already number), so replacement is guaranteed-equivalent.",
    "notFiled_lowValue": "isFinite sibling rule candidate (0 corpus sites); alias-binding FN (0 sites); template-literal computed prop globalThis[`isNaN`] FN (contrived); optional-chain globalThis?.isNaN is already handled."
  },
  "priorOpenIssues_stillValid_notRefiled": {
    "43326": "await-summary FN: discarded promise in non-bare-call statement positions (my 2026-07-04 FN3, landed as issue, OPEN)",
    "42680": "setoutput nonStringKind Literal only, misses UnaryExpression -1/+5",
    "42682": "setoutput only .length not .number/.size",
    "42189/42915": "catch/promise-catch whole-block branch-order FN",
    "42188/42190": "broaden catch beyond message/stack/code; add suggestions + computed access",
    "async-entrypoint (2 filed 2026-07-03)": "arrow/fn-expr + .then-no-catch FN; scope-blind bare-name Set FP",
    "42015/42016/42017/41962/41963": "parseInt+json alias/computed/deferred-callback out of scope or fix asks"
  },
  "note_issueLimit": "create_issue max 3/run. Filed 2 this run (both prefer-number-isnan). Chose quality over cap.",
  "nextPriorities": [
    "Re-read src before refiling: repo ships new rules fast (9th rule appeared since 2026-07-04; await-summary alias fix landed same-day-ish as #43404).",
    "Watch whether prefer-number-isnan gains provably-numeric autofix; if so, corpus becomes --fix-able for ~51 sites.",
    "All 9 known rules now reviewed at least once. Next: mine a genuinely NEW defect class. Candidates: core.setFailed non-string (twin of setOutput); global isFinite (0 sites today - skip); Array.prototype.includes-on-NaN; unchecked fs.promises rejections; String.prototype.replace w/o /g on repeated tokens.",
    "Consider re-sweeps of oldest-reviewed rules (require-json-parse-try-catch, require-parseInt-radix) for regressions after heavy refinement churn.",
    "Cannot close issues (safe outputs: create_issue/create_discussion/missing_tool/missing_data/noop/push_repo_memory). Close-recs -> discussion only."
  ],
  "ops": [
    "repo-memory readable+writable via Read/Write/Glob tools ONLY (Bash ls/cat/find BLOCKED on /tmp - scoped to working dir). Write works for /tmp/gh-aw/repo-memory.",
    "create issue: Write JSON {title,body,labels} to a file INSIDE working dir (e.g. eslint-factory/.tmp_issue.json - /tmp/gh-aw/agent write via Bash redirect is BLOCKED), then `cd <dir> && safeoutputs create_issue . < file` (2>/dev/null suppresses bridge log). {\"result\":\"success\"}==accepted. rm temp files after.",
    "dedupe: `gh api \"repos/github/gh-aw/issues?state=open&per_page=100\" --jq '.[]|select(...)'`. NOTE: gh api search/issues returned a GHOST result (total_count=1, all item fields null) for isNaN queries - an indexing artifact, NOT a real dup. Use the issues endpoint + jq title filter instead. `gh search issues --json` throws 'malformed version'.",
    "Bash blocks compound cmds (&&, ;, echo-chains, $() expansion, $var in some contexts) needing approval - run single commands. sed -n 'A,Bp' works for context. grep -rn ok.",
    "eslint-factory has NO node_modules/dist; npm firewalled - cannot run linter live. Ground findings statically via grep on .cjs + reading rule src + tests.",
    "patch limit ~10KB per push - keep state.json lean, prune old verbatim finding lists."
  ]
}
