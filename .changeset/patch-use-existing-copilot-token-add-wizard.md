---
"gh-aw": patch
---

Default add-wizard to use an existing `COPILOT_GITHUB_TOKEN` secret, improve workflow and Copilot authentication prompt formatting, and support `--gh-aw-ref` in both `add` and `add-wizard`.