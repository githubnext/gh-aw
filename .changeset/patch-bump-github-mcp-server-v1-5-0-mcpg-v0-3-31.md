---
"gh-aw": patch
---

Bump the default GitHub MCP Server version to `v1.5.0` and the default `gh-aw-mcpg` version to `v0.3.31`, then regenerate workflow lock files so compiled workflows pick up the refreshed container images.
