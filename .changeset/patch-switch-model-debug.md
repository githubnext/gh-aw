---
"gh-aw": patch
---

Switch model used for debugging and testing (internal/debug change).

This is an internal change that adjusts which model is used for
debugging and smoke tests. No user-facing behavior or CLI API is
changed.

