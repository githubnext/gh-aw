---
"gh-aw": patch
---

Add support for `imports.marketplaces` and `imports.plugins` in workflow frontmatter, including merge/dedup across shared imports and GitHub tree URL normalization for plugin install specs.
