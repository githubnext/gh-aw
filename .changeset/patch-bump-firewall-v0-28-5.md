---
"gh-aw": patch
---

Bump the default `gh-aw-firewall` (AWF) version from `v0.28.4` to `v0.28.6`, which includes hardened sandbox container startup retry and diagnostics for transient Docker mount failures, and recompile workflow lock files to use the updated default.
