---
"gh-aw": patch
---

Add the default `--deny-tool fetch_copilot_cli_documentation` flag to the
Copilot engine so Copilot agents cannot access their own documentation tool
by default. This updates the generated Copilot CLI arguments and corresponding
workflow lockfiles.

