---
"gh-aw": patch
---

Verified that the `@github/copilot` CLI dependency is already at the latest stable
release (`0.0.365`) — no code changes required.

Reference: `pkg/constants/constants.go:33`.

