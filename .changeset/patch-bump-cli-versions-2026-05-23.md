---
"gh-aw": patch
---

Bump the default CLI versions to Claude Code `2.1.150`, Copilot CLI `1.0.51`, Codex `0.133.0`, and GitHub MCP Server `v1.0.5`, then recompile workflow lock files so generated workflows pick up the new defaults.
