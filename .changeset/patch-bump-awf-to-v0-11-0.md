---
"gh-aw": patch
---

Bumps the default `gh-aw-firewall` (AWF) version from `v0.10.0` to `v0.11.0`.

This change updates the `DefaultFirewallVersion` constant and the
corresponding tests and recompiled workflow lock files.

