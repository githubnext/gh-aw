---
tools:
  cli-proxy: true
  github:
    mode: gh-proxy
    toolsets: [default]
---

<!--
## GitHub Proxy Default Tooling

This shared workflow enables the default GitHub CLI proxy tool configuration:

```yaml
imports:
  - shared/github-proxy-default.md
```

Equivalent inline configuration:

```yaml
tools:
  cli-proxy: true
  github:
    mode: gh-proxy
    toolsets: [default]
```
-->
