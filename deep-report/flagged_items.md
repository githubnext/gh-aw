## Flagged Items (2026-08-14)

- **[reliability, resolved, 3rd consecutive verified-fix cycle]** Shared PR-review infra flakiness (#52518, tracked since 2026-08-13) — Test Quality Sentinel/Matt Pocock/Ponytail Reviewer all now 15/15 (100%) in fresh samples. Commented on #52518 recommending closure. Watch whether it's actually closed next cycle, or recurs on a 4th date (would reopen the investigation).
- **[reliability, new, cross-verified]** Design Decision Gate merge-blocking failure/cost hotspot — 3 independent monitors (audit-workflows, copilot-session-insights, api-consumption) converged on this workflow this cycle with no pre-existing root-cause issue. Filed. Watch next cycle whether it gets investigated or the failure rate persists/worsens.
- **[process, verified-fixed, holding]** Firewall hostname bug fix (commit b2ef1f3/#52377) — 3rd consecutive cycle directly verified against live data (0 blocks, 0 wrong-hostname requests). Can likely stop spot-checking this specific item now that it's held for 3 cycles; worth one more light check next cycle then retire from the watchlist.
- **[tooling, chronic, NOT re-filed]** `agenticworkflows logs` ~60s hard timeout (ignores `--timeout`, ~40-run ceiling) — 9th confirmed occurrence across cycles, 8 prior issues closed without a durable fix. Decision this cycle: do not file a 9th duplicate; instead flagged prominently in the report body as a standing constraint. Reconsider next cycle whether a different escalation path (e.g., pinging a maintainer directly, or a "keep open until CI-verified fixed" issue template) would break the file-close-recur cycle.
- **[code quality, process lesson]** Two issues that were closed but not actually fixed, caught by direct source verification: `getParsedSchemaDoc` any-type (#50678) and `RunSummary`/`DownloadResult` duplication (#47387/#47439). Both re-filed with explicit notes. **New standing practice: before treating a candidate task as "already covered" by a closed issue, spot-check the actual source file — closed status alone is not sufficient evidence a fix landed.**
- **[monitoring meta-theme, new]** Three independent monitoring agents (audit-workflows 38 days, sergo 5 weeks, eslint-refiner since 07-08) reported their own repo-memory as stale/gapped this cycle. Not yet a filed issue (no single fix location identified) but worth watching for a 4th recurrence next cycle, which would justify a `push_repo_memory`-reliability investigation issue.
- 7/7 planned issues filed this cycle (all dedup-checked against open issues via `gh api search/issues` — confirmed `gh search issues`/`gh issue list -S` remain broken with "malformed version" in this environment; the `gh api "search/issues?q=..."` workaround from prior cycles still works). Plus 1 add_comment on #52518. Full detail in known_patterns.md.
