## Flagged Items (2026-08-12)

- **[infra, escalating]** `agenticworkflows logs` MCP tool timeout is now confirmed across 2 consecutive DeepReport cycles (2026-08-11, 2026-08-12) plus a separate prior report (#51952, `engine`-filter variant) that auto-expired without a real fix. This cycle: `count:100`+`start_date` timed out at ~60s, `count:30` alone succeeded in ~37s. Re-filed with fresh evidence citing #51952. Watch: does the new filing get a real PR-linked fix, or TTL-expire silently again like #51952 did?
- **[reliability, carried forward]** Fleet-wide 49% agent-job failure rate (#51935, 2026-08-11) still has no formal reconciliation. Team-evolution report (#52145) this cycle offers a plausible explanation (dominated by already-known-broken workflows, not new regression) but this hasn't been confirmed/closed by the investigation issue itself yet.
- **[process, good news]** `strict:` mode docs fix (#52086) verified landed for real this cycle — direct read of `frontmatter.md` confirms accurate content. First non-chronic resolution observed in several cycles; worth continuing to spot-check 1-2 prior "closed" issues per cycle this way rather than trusting closure status alone.
- **[operational]** Sergo (#52232) lost its real finding (perf-linter path-matching bug) to a quota-exhaustion incident — 3 placeholder `create_issue` test calls burned the run's entire quota before the actual finding could be filed. DeepReport filed it this cycle on Sergo's behalf. Worth flagging to whoever owns Sergo's workflow: guard test/placeholder `create_issue` calls behind a dry-run flag so they don't compete with real findings for quota.
- **[docs]** New user experience gap found and filed: AI issue triage guide's example uses non-default GitHub labels without noting they must pre-exist (#52088) — a first-run-failure class of bug, same pattern as needing this kind of scrutiny more broadly across other example workflows in docs.
- 7 new issues filed this cycle (all dedup-checked against 126 open issues first, no exact matches found): perf-linter coverage-gate silent no-op, firewall hostname bug (PR Code Quality Reviewer), 8 workflows missing gh-aw-detection, schema-consistency tooling + frontmatter-full.md doc gaps, GitHubToken field-shadowing bug, logs-tool timeout recurrence, label pre-creation doc gap. Full detail in known_patterns.md.
- LintMonster, Cache Strategy, and ESLint Refiner all self-filed their own findings as issues this cycle — cross-checked against this cycle's 7, no duplicates.
