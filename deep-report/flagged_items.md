## Flagged Items (2026-08-13)

- **[process, good news, 2nd consecutive]** Firewall hostname bug (`api.individual.githubcopilot.com`) verified fixed via commit `b2ef1f3`/#52377 — directly confirmed against live firewall log data (246/246 requests to correct hostname, 0 blocked) for PR Code Quality Reviewer. Second cycle in a row with a directly-verified real fix landing (first was #52086 strict-mode docs). Keep spot-checking prior fixes each cycle.
- **[reliability, likely resolved]** The 49% fleet-wide failure figure from 2026-08-11 (#51935) does not match any of 3 independent measurements this cycle (82.5%/84.6% from a 40-run logs sample, 19.2% run-weighted from #52386's first baseline, 85.9% from #52375). Recommend the original investigation issue be closed with this convergent evidence if still open next cycle — watch for whether it gets a formal reconciliation or stays open indefinitely (would repeat the #51807-style chronic-non-closure pattern otherwise).
- **[security-adjacent, new]** `pkg/intent/policy.go` PolicyCompiler doesn't validate Autonomy/WriteScope against its own rank tables when seeding from the first matching rule — currently advisory-only (not wired to runtime enforcement per the code's own WARNING comment) but should be closed before `Authorizer.AuthorizeTool` integration ships, or a config typo could silently fail open. Filed as #aw_policy1 this cycle.
- **[tooling gap, new]** Sentrux's own `god_files_ceiling` rule isn't being enforced — `sentrux check` reports "0 rules checked" despite a live 3-vs-1 ceiling breach, and the daily report still says "All rules pass." Watch next cycle whether this gets a real fix (CLI flag / config wiring) vs. silently persisting.
- **[reliability, correlated pattern]** 3 independent PR-review agents (Test Quality Sentinel, Matt Pocock, Impeccable Skills Reviewer) show correlated failure-rate dips on the exact same 3 dates (Aug 7/11/13), all sharing `shared/pr-diff-data-fetch.md` + copilot engine + cli-proxy — strongly suggests shared infra flakiness rather than 3 independent prompt regressions. Filed as an investigation task; watch whether next cycle's data shows the same correlated pattern recurring (would confirm shared root cause) or diverges (would argue for per-agent fixes instead).
- **[process, self-inflicted]** A shell quoting bug this cycle (backticks in `jq -n '...'` inline JSON being misinterpreted as command substitution) wasted one of the 7 `create_issue` slots on an accidental duplicate-retry, leaving no room to file the 7th candidate (Ponytail Reviewer prompt gaps). Fixed by switching to write-file + `cat | safeoutputs` for the remainder of this cycle's filings — use that approach by default going forward whenever a body contains backticks/code spans. Ponytail Reviewer issue carries forward to next cycle as still-unfiled.
- 6 of 7 planned issues filed this cycle (all dedup-checked against open issues via `gh api search/issues` first — note `gh search issues` and `gh issue list -S` are both broken in this environment with a "malformed version" error; use `gh api "search/issues?q=..."` instead). Full detail in known_patterns.md.
