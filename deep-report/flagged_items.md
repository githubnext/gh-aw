## Flagged Items (2026-08-17, 18:23Z cycle)

- **[new, filed, highest priority]** Deep-report's own discussions/issues fetch caches by calendar day while the workflow runs every 6h — 3 of 4 daily runs silently reuse stale data. This is *why* the 06:26Z and 12:22Z cycles reported "zero new activity." Filed fix. Watch: does the fix key the cache by run bucket (or remove cross-run caching) so future cycles get genuinely fresh data every time.
- **[new, filed]** CI failing on `main` right now — safe-outputs config env-var→config.json migration broke `TestCreatePullRequestCrossRepoCheckout` and `TestSafeOutputsMCPServer*` tests. Live regression, not flaky. Filed. Watch: does this get fixed before more commits land on top.
- **[new, filed]** Schema/docs drift bundle — `github-app` missing from JSON schema, `max-runs`/`max-turns` untyped, `user-rate-limit` undocumented (from discussion #53313). Filed as one task.
- **[new, filed]** Large-file decomposition — `pkg/workflow/cache.go` (1118 lines) and `dependabot.go` (1072 lines) have clean split seams per discussion #53391. Filed.
- **[new, filed]** Recurring GitHub Remote MCP toolset unavailability, 3rd+ occurrence, prior 2 issues (#51703, #52245) auto-expired without a fix. Filed as non-expiring tracking issue. Watch: does it recur a 4th time before anyone investigates.
- **[new, commented]** #53263 (safe_outputs job hard-fails on one non-retryable error) — added 2 new run IDs + the `failure_kind` misclassification insight from discussion #53295 (Safe Output Health Monitor's first run). Not filed separately since #53263 already covers the exact root cause.
- **[chronic, unchanged]** #53180 (0-turn Copilot CLI driver_exit) — not re-checked this cycle (workflow-log sampling deferred, see known_patterns.md).
- **[meta, do-not-repeat, not re-verified this cycle]** "Label the unlabeled open issues" — same 6-issue backlog as of 12:22Z cycle (#53204, #53136, #52723, #52608, #52575, #52547). Issues-analyst pass skipped this cycle; re-verify next cycle once cache fix lands.
- **[carried forward, unchecked — no relevant report appeared this cycle]** Design Decision Gate `pr_number` bug (filed 2026-08-16); Sentrux `api.sentrux.dev` regression 3rd attempt (filed 2026-08-16); MCP `rpc-messages.jsonl` type-field investigation (filed 2026-08-17); Cache Strategy Analyzer detection fix (filed 2026-08-17); Avenger chronic driver_exit 5th filing (filed 2026-08-17); Copilot PR prompt-writing guidance (filed 2026-08-17); audit-workflows 41-day-gap meta-finding (filed 2026-08-17); Serena Go LSP toolchain gap generalization (filed 2026-08-17 12:22Z cycle). None had a fresh source report this cycle to re-verify against — the ~63 fresh discussions this cycle were only sampled, not exhaustively mined (see processed-discussions.md).
- **[process note]** This cycle prioritized root-causing why 3 prior cycles went "quiet" over exhaustively mining the ~63 newly-surfaced discussions. Next cycle should do a full discussion-mining pass now that the stale-cache trap is understood and documented.
