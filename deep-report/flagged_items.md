## Flagged Items (2026-08-17)

- **[reliability, new, high-confidence]** Cache Strategy Analyzer's jq detection checks a field (`.tools.cache_memory`) that `generate_aw_info.cjs` never writes into `aw_info.json` — structurally broken since inception (or since a regression before 2026-08-11, when it last correctly found a real issue via #52134). Filed. Watch whether the fix actually lands (verify against source, not just issue-closed status) next cycle, and whether the analyzer starts finding real cache-miss issues across the ~101 cache-memory workflows once fixed.
- **[reliability, chronic, 4th filing]** Avenger `err-config-no-structured-logs` driver_exit — 19 recurrences, 4 prior closures didn't hold. Filed with the audit's own "consider disabling schedule" recommendation attached. If a 5th closure doesn't hold either, recommend the maintainers seriously consider the disable-schedule path rather than another agent-filed reopen.
- **[process, new meta-category]** `audit-workflows` itself missed its schedule for 41 days with zero alerting anywhere in the fleet. Filed with a suggested heartbeat-check shape. Watch whether (a) root cause of the missed schedule gets diagnosed, (b) any heartbeat-style check gets built, (c) it recurs a 2nd time.
- **[process, data-backed, multi-week]** Prompt-writing guidance for Copilot-assigned issues — 6+ weeks of consistent trend data (concise/concrete prompts merge more). Filed as a quick docs task. Check next cycle if it landed, and whether merge-rate (currently declining 81.2%→76.7%) responds.
- **[observability, hedged/unconfirmed]** MCP `rpc-messages.jsonl` may lack the top-level `type` field our parser (`gateway_logs_rpc.go`) expects — filed explicitly as "needs verification," not a confirmed bug. **Re-verify next cycle** by pulling one real raw `rpc-messages.jsonl` sample and diffing its actual fields — this is the single highest-priority "unchecked assumption" to resolve next time, since it could mean either (a) a real silent metrics-degradation bug, or (b) the observability report's own check is wrong, and neither has been distinguished yet.
- **[resolved this cycle, retire]** #53238 (new Avenger Bun-segfault recurrence) — commented onto #51984 as duplicate, don't re-file. #53180 (0-turn crash spreading) — updated with this cycle's rotated-workflow evidence, no new issue needed.
- **[unchecked this cycle, carry forward]** `agenticworkflows logs` ~40-run timeout ceiling (still not re-tested at high `--count` across 2 cycles now) — genuinely deprioritize or actually test next cycle, don't let this keep rolling over indefinitely. Also: Design Decision Gate pr_number bug (filed 2026-08-16, not re-verified since no relevant report appeared this cycle) and Sentrux `api.sentrux.dev` firewall regression (same — no sentrux report in this cycle's window) — re-check both next cycle when/if their reports reappear.
- **[benign, verified, not filed]** smoke-copilot Google-domain firewall blocks (Chromium/Playwright telemetry, not a real need); safeoutputs.jsonl absence in zero-safe-output runs (expected by design); weak firewall deny-path visibility (likely just means no blocks occurred).
- 5/5 planned issues filed this cycle + 2 comments (all dedup-checked via `gh api search/issues`; `gh search issues`/`gh issue list -S` remain broken in this environment). Full detail in extracted-tasks.md/known_patterns.md.
