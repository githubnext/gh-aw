## Flagged Items (2026-08-11)

- **[P0/reliability]** Fleet-wide agent-job failure rate hit 49.0% (103/210 runs) in the 2026-08-11 Safe Output Health Monitor's window — far above the prior 10% spot-sample baseline. Filed an investigation issue since no monitor owns the aggregate fleet-wide number (only per-workflow issues exist). Watch next cycle: does the investigation reconcile how much is already-known-chronic (PR Sous Chef, Issue Monster, Copilot CLI segfault #51789) vs. genuinely new?
- **[infra]** `agenticworkflows logs` tool timed out twice this cycle (`context deadline exceeded` at exactly ~60s, both with `count:100` and `count:50` + explicit `timeout:120` param — the param appears not to be honored server-side). Did not attempt a 3rd variant per retry-limit guidance; relied on same-day discussion reports (safe-output-health, agent-performance, security-observability, daily-issues) as a substitute secondary source this cycle. If this recurs next cycle too, worth flagging as its own infra issue — not filed yet since two consecutive-cycle occurrences isn't confirmed.
- **[P0 process, carried forward]** DeepReport repo-memory persistence path fix from 2026-08-10 is holding as a workaround (see known_patterns.md) but the underlying source-file fix in `.github/workflows/deep-report.md` has no verified-merged evidence yet. Keep watching for a cited PR SHA.
- Copilot Session Insights: conversation-transcript gap now described as "~4.5 months" (since ~2026-03-20) by the reporting agent itself — longer than the "44+ days" figure from 2026-08-10. Inconsistent counter across cycles; not re-filed as a new issue, folded into the existing process-gate issue as evidence of the same "closed without verified fix" pattern.
- 7 new code-quality/reliability issues filed this cycle (all dedup-checked against open issues first, no matches found): strict-mode docs inversion, user-rate-limit schema gap, README Claude-engine default trap, JobStep/JobStepData dup, log-entry-struct base type, compiler_types.go split, 49%-failure-rate investigation. Full list in known_patterns.md and extracted-tasks.md.
- Sergo, LintMonster, and ESLint Refiner all self-filed their own findings as issues this cycle — cross-checked and none duplicated in this cycle's 7.
