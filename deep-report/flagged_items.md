## Flagged Items (2026-08-16)

- **[reliability, new]** Design Decision Gate `pr_number`/workflow_dispatch hard-fail — distinct from the already-fixed invocation-cap bug (#52836). Filed fresh this cycle. Watch whether it gets a real fix (verify against source, not just issue-closed status) next cycle.
- **[reliability, watch]** Fleet reliability jump to 96.93%/97.26% — partly attributable to audit-workflows catch-up sampling after a 40-day gap, not fully attributable to a specific fix. Confirm this holds with a 2nd data point next cycle before trusting it as the new baseline.
- **[reliability, new, small sample]** 0-turn Execute CLI crash signature now appearing on Aider (first) and Crush (2 first) in addition to Copilot. Filed as investigation. Watch whether this grows or stays noise-level next cycle.
- **[process, new regression, 3rd fix attempt]** `api.sentrux.dev` missing from daily-sentrux-report.md network.allowed — 2 prior closed fixes (#43655, #40546) didn't hold. Filed. If this regresses a 4th time, consider a different fix approach than "add domain to allowlist" (e.g. a lint rule that fails compilation if a referenced MCP tool's domains aren't in network.allowed).
- **[process, chronic, deliberately not re-filed, 16 occurrences]** Docs "jargon before first use" complaint — filed/closed 15+ times since February without durable fix. Flagged in report body only. Consider recommending a different escalation (direct maintainer ping, or a standing "won't fix via agent" label) if this persists.
- **[process lesson, reinforced]** Standing practice "closed ≠ fixed, verify source" caught 2 more stale closures this cycle (Design Decision Gate pr_number bug, Sentrux firewall regression), and correctly prevented 1 false-positive filing (Anthropic WIF claimed undocumented — actually is, at auth.mdx:220).
- **[resolved, retire from watchlist]** #52518 PR-review infra flakiness (closed), monitoring-staleness meta-theme (resolved via catch-up, didn't recur), Sentrux god_files_ceiling enforcement gap (now actively firing).
- **[unchecked this cycle, re-verify next]** Firewall hostname fix (was slated for retirement after 3 clean cycles, not re-checked) and `agenticworkflows logs` ~40-run timeout ceiling (not re-tested at high `--count` this cycle).
- 7/7 planned issues filed this cycle (all dedup-checked via `gh api search/issues`; `gh search issues`/`gh issue list -S` remain broken in this environment). Full detail in extracted-tasks.md/known_patterns.md.
